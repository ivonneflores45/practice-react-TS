# React + TypeScript + Vite + Framer motion + SVG icons

## what?
first attempt at making cool interactive background using framer motion to move SVG and little blobs
****
`AnimatedBackground.tsx`: moves SVG icons, blobs, and dots w/ framer motion

`BackgroundElements.tsx`: has all the 'blobs', dots, SVGs in there. might redo this because it looks messyyyyy. using SVG from bootstrap icon. theyre kinda ugly but theyre there